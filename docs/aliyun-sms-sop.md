# EBM中转站阿里云短信服务 SOP

本文用于 EBM中转站（基于 New API）的中国大陆注册验证码短信开通、上线、巡检、故障处理和密钥轮换。它不保存任何 AccessKey 明文；所有密钥只能存放在受控的密码管理工具和 Coolify 环境变量中。

## 1. 适用范围与成功标准

适用场景：向中国大陆手机号发送注册验证码，模板变量名固定为 `code`。

只有同时满足以下条件，才可将短信注册视为正式可用：

1. 企业资质已审核通过，短信签名已关联正确的企业资质。
2. 签名通过阿里云平台审核，并完成移动、联通、电信三网的运营商实名制报备验证。
3. 验证码模板审核通过，模板变量为 `code`。
4. 专用 RAM 用户只拥有 `dysms:SendSms` 权限，AccessKey 状态正常。
5. Coolify 中五项环境变量齐全，重新部署后 `/api/status` 返回 `sms_verification_ready: true`。
6. 使用移动、联通、电信号码各完成至少一次真实收件测试，发送记录显示成功，短信签名、内容和验证码均正确。
7. 邮箱验证保持可用，作为短信故障时的注册兜底方式。

> `sms_verification_ready: true` 只表示应用环境变量齐全，不代表余额、签名报备、模板、RAM 权限或运营商通道一定正常。最终以上线前的真实收件测试为准。

## 2. 当前生产配置快照

以下快照记录于 2026-08-24，后续以阿里云和生产环境的实时状态为准。

| 项目 | 当前值 |
| --- | --- |
| 应用 | EBM中转站 |
| 生产地址 | `https://ai.ebm001.com` |
| 阿里云短信签名 | `苏州橡数数字科技` |
| 模板名称 | `New API注册验证码` |
| 模板 Code | `SMS_511735352` |
| 模板变量 | `code`，仅数字 |
| RAM 用户 | `ebm-sms-sender` |
| RAM 自定义策略 | `EbmSmsSendOnly` |
| Region ID | `cn-hangzhou` |

当前运营商报备状态为移动、联通、电信均“已报备待验证”。实际发送记录出现：

```text
签名实名制报备问题。应运营商要求，各短信发送通道需完成对应签名的实名制报备。(386)
```

这不是“余额不足”的证据，而是当前签名尚未通过对应发送通道的实名制报备。正式开放短信注册前，必须完成第 8 节的三网真实收件验收；在此之前保留邮箱注册，并建议关闭短信验证开关。

## 3. 端到端执行总表

按下表从上到下执行。签名的运营商实名制报备是异步流程，可在等待期间继续申请模板、配置 RAM 和部署应用；最终必须回到步骤 10 完成三网真实收件闭环。

| 步骤 | 操作位置 | 操作 | 本项目填写值 | 通过标准 |
| --- | --- | --- | --- | --- |
| 0 | 阿里云账号、短信服务 → 资质管理 | 完成企业实名认证、开通短信服务、提交企业资质 | 企业主体以营业执照为准 | 企业资质“审核通过” |
| 1 | 国内消息 → 签名管理 → 添加签名 | 申请短信签名并关联企业资质 | `苏州橡数数字科技` | 阿里云平台审核通过，签名“可用-正常” |
| 2 | 签名管理 → 运营商报备结果 | 跟踪移动、联通、电信实名制报备 | 使用步骤 1 的签名 | 三网无“报备失败”；进入“已报备待验证”后继续后续步骤 |
| 3 | 国内消息 → 模板管理 → 添加模板 | 申请注册验证码模板 | 变量 `${code}`，有效期 10 分钟 | 模板审核通过并取得 `SMS_511735352` |
| 4 | RAM → 身份管理 → 用户 | 创建专用程序用户 | `ebm-sms-sender` | 用户存在，不使用主账号 AK，不开多余权限 |
| 5 | RAM → 权限管理 → 权限策略 | 创建并绑定最小权限策略 | `EbmSmsSendOnly`，仅 `dysms:SendSms` | RAM 用户只拥有发送所需权限 |
| 6 | RAM 用户 → 认证管理 | 创建 AccessKey | AK 值不写入本文档 | AK 为启用状态，并已安全保存 |
| 7 | Coolify → 应用 → Environment Variables | 配置五项服务端环境变量 | 见第 6 节 | 五项齐全，密钥未进入代码或前端 |
| 8 | Coolify → 应用 | 重新部署 | 目标提交和版本以发布记录为准 | 部署健康，`sms_verification_ready=true` |
| 9 | EBM中转站 → 系统设置 → 身份验证 | 保持邮箱验证开启；短信先关闭，验收时受控开启 | 邮箱开启、短信待验收 | 注册页仍有邮箱兜底 |
| 10 | 注册页、阿里云发送记录、三网手机 | 移动、联通、电信逐一真实发送、收件、提交验证码 | 三网测试号码各一个 | 三网均成功，发送记录无报备/权限/模板/欠费错误 |
| 11 | EBM中转站 → 身份验证 | 正式启用短信验证 | 邮箱和短信同时开启 | 用户可选择邮箱或短信完成注册 |

执行过程中必须保存以下非敏感记录：操作日期、执行人、签名名称、模板 Code、RAM 用户名、策略名、应用版本、脱敏测试号码、运营商、阿里云 Request ID 和验收结果。禁止记录 AccessKey Secret 和验证码。

## 4. 阿里云侧：资质、签名和模板

### 4.1 开通短信服务并准备企业资质

1. 使用企业实名认证的阿里云账号开通短信服务。
2. 进入“国内消息 → 资质管理”。
3. 新增或选择企业资质，确认状态为“审核通过”。
4. 核对企业名称、统一社会信用代码、证件有效期、管理员和联系电话等信息。
5. 不使用个人资质承载企业商业短信。

本步骤通过标准：资质列表中存在与短信签名主体一致、状态为“审核通过”的企业资质。

### 4.2 申请签名并触发运营商实名制报备

控制台路径：“国内消息 → 签名管理 → 添加签名”。

1. 签名来源选择与企业资质相符的“企事业单位全称或简称”。
2. 选择第 4.1 节审核通过的企业资质。
3. 签名名称填写 `苏州橡数数字科技`。
4. 使用场景填写账号注册验证码，不填写营销用途。
5. 按控制台要求补充营业执照或其他证明材料并提交审核。
6. 审核通过后记录签名名称，环境变量必须逐字使用该名称，不带短信中显示的方括号。
7. 打开该签名的“运营商报备结果”，分别查看移动、联通、电信状态和具体原因。
8. 状态为“已报备待验证”时，不把它误判为已完成；继续申请模板、配置应用，并在第 8 节用对应运营商号码完成真实发送验证。

阿里云官方提示，运营商实名报备平均需要 5–7 个工作日，部分情况需要 7–10 个工作日，且实际可能更长。新业务应至少提前 10 个工作日办理。报备未完成时不得承诺短信注册已上线。

本步骤通过标准：阿里云平台审核通过、签名状态“可用-正常”、签名已关联正确企业资质，三网报备无明确失败。三网最终通过标准以第 8 节真实收件验收为准。

### 4.3 申请验证码模板

控制台路径：“国内消息 → 模板管理 → 添加模板”。

1. 模板类型选择“验证码”。
2. 模板名称填写 `New API注册验证码`。该名称用于控制台识别，不会直接发送给用户。
3. 关联签名选择 `苏州橡数数字科技`。
4. 申请说明填写 EBM中转站用户注册时验证中国大陆手机号。
5. 模板变量使用 `${code}`，变量名必须与应用发送参数完全一致，变量规则选择仅数字。
6. 模板内容填写下方文案，不添加营销信息、链接、域名或联系方式。
7. 提交审核，审核通过后复制模板 Code；不要把模板名称误填到 Code 环境变量中。

本项目当前模板内容：

```text
您正在进行账号注册，验证码为${code}，10分钟内有效。请勿向他人泄露，如非本人操作请忽略。
```

本步骤通过标准：模板状态“审核通过”，模板 Code 为 `SMS_511735352`，变量名为 `code`，变量类型允许 6 位数字。

## 5. RAM 身份认证与 AccessKey

### 5.1 创建专用程序用户

控制台路径：“RAM 访问控制 → 身份管理 → 用户 → 创建用户”。

1. 登录名称填写 `ebm-sms-sender`。
2. 显示名称可填写 `EBM SMS Sender`。
3. 访问方式只保留 OpenAPI/AccessKey 所需的程序访问能力，不开启不必要的控制台登录。
4. 创建后进入该用户的权限管理页。
5. 不使用阿里云主账号 AccessKey，也不与其他系统共用该 RAM 用户。

当前用户：`ebm-sms-sender`。

### 5.2 创建并绑定最小权限策略

控制台路径：“RAM 访问控制 → 权限管理 → 权限策略 → 创建权限策略 → 脚本编辑”。

创建自定义策略 `EbmSmsSendOnly`，内容如下：

```json
{
  "Version": "1",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["dysms:SendSms"],
      "Resource": ["*"]
    }
  ]
}
```

保存策略后返回 `ebm-sms-sender` 的权限管理页，单击“新增授权”，只绑定 `EbmSmsSendOnly`。该用户无需 `AliyunDysmsFullAccess`、`AdministratorAccess` 或其他管理权限。

本步骤通过标准：用户权限列表只包含业务明确需要的策略，策略允许 `dysms:SendSms`，没有额外管理权限，也没有会覆盖 Allow 的 Deny 规则。

### 5.3 创建和保管 AccessKey

控制台路径：“RAM 访问控制 → 身份管理 → 用户 → `ebm-sms-sender` → 认证管理 → AccessKey”。

1. 为专用 RAM 用户创建 AccessKey。
2. 创建时立即存入受控的密码管理工具；AccessKey Secret 通常无法再次查看。
3. 不把 AccessKey 写入代码、Git、文档、截图、聊天、工单或构建日志。
4. 只在 Coolify 的服务端环境变量中配置密钥，并确保变量不会暴露到前端构建参数。

本步骤通过标准：AccessKey 属于 `ebm-sms-sender`、状态为启用、密钥有安全副本，代码仓库和文档中不存在真实密钥。

## 6. Coolify 配置与部署

目标应用：`new-api:main-g66uvs0paf4e5qiv3hugjuaz`。进入该应用的“Environment Variables”，确认操作的是生产环境而不是其他项目或预览环境。

在 EBM中转站应用的环境变量中配置：

```dotenv
ALIBABA_CLOUD_ACCESS_KEY_ID=<RAM用户的AccessKey ID>
ALIBABA_CLOUD_ACCESS_KEY_SECRET=<RAM用户的AccessKey Secret>
ALIYUN_SMS_REGION_ID=cn-hangzhou
ALIYUN_SMS_SIGN_NAME=苏州橡数数字科技
ALIYUN_SMS_TEMPLATE_CODE=SMS_511735352
```

操作要求：

1. 分别新增五项变量，变量名大小写必须与示例完全一致。
2. `ALIBABA_CLOUD_ACCESS_KEY_ID` 和 `ALIBABA_CLOUD_ACCESS_KEY_SECRET` 填写第 5.3 节创建的同一组密钥，并按敏感变量管理。
3. `ALIYUN_SMS_SIGN_NAME` 填签名文字，不带 `【】`。
4. `ALIYUN_SMS_TEMPLATE_CODE` 填审核后的 `SMS_...` Code，不填模板名称。
5. 环境变量只提供给服务端运行环境，不放入前端公开变量。
6. 保存五项变量后执行重新部署；仅重启旧容器不一定会加载新配置。
7. 部署日志中不得打印 AccessKey。
8. 部署成功后检查：

```bash
curl -fsS https://ai.ebm001.com/api/status
```

实际响应使用 `success` 和 `data` 包装，至少应包含：

```json
{
  "success": true,
  "data": {
    "system_name": "EBM中转站",
    "sms_verification_ready": true
  }
}
```

如果 `sms_verification_ready` 为 `false`，先检查五项变量是否齐全和部署是否使用了最新环境变量，不要直接反复申请新密钥。

本步骤通过标准：Coolify 部署状态为健康，生产 `/api/status` 返回 `success=true` 和 `data.sms_verification_ready=true`。这只代表配置齐全，不能替代第 8 节真实发送验收。

## 7. 应用开关与受控测试

访问 `https://ai.ebm001.com/system-settings/auth/basic-auth`，或从后台进入“系统设置 → 身份验证 → 基本身份验证”：

- 邮箱验证：保持开启，作为兜底方式。
- 短信验证：配置阶段保持关闭；执行第 8 节时受控开启，任何运营商失败就再次关闭；全部验收通过后正式开启。

应用行为：

- 邮箱和短信同时开启：注册页显示两种验证方式，用户可以选择。
- 只开启邮箱：短信故障或报备期间的推荐状态。
- 只开启短信：不推荐，短信供应商或运营商故障会直接阻断注册。

本步骤通过标准：邮箱验证始终可用；短信验证只在有值守人员和三网测试号码的验收窗口开启。

## 8. 三网发送、收件与注册验收

### 8.1 测试准备

准备移动、联通、电信的中国大陆手机号各一个，号码应由测试人员本人控制并同意接收测试短信。记录测试时间、运营商、脱敏手机号、发送结果、阿里云 Request ID 和实际收件结果。

### 8.2 逐网测试

对每个运营商依次执行：

1. 在注册页选择短信验证并输入测试手机号。
2. 获取验证码一次，等待页面返回结果。
3. 在阿里云“发送记录”中按发送时间和脱敏号码查找本次请求，记录状态、失败原因和 Request ID。
4. 确认手机在合理时间内收到短信。
5. 核对短信签名、文案、6 位验证码和 10 分钟有效期提示。
6. 使用收到的验证码完成一次测试注册；测试账号按内部规则清理。

不要连续点击发送。本项目限流为：

- 同一手机号：每分钟 1 条、每小时 5 条、每天 10 条。
- 同一 IP：每分钟 10 次请求。

### 8.3 上线门禁清单

- [ ] 企业资质审核通过且信息正确。
- [ ] 短信签名关联正确的企业资质。
- [ ] 移动真实发送和收件成功。
- [ ] 联通真实发送和收件成功。
- [ ] 电信真实发送和收件成功。
- [ ] 模板审核通过，变量名为 `code`。
- [ ] RAM 用户只绑定发送所需的最小权限。
- [ ] `/api/status` 中 `sms_verification_ready` 为 `true`。
- [ ] 阿里云发送记录无报备、签名、模板、权限或欠费错误。
- [ ] 邮箱验证可正常使用。
- [ ] 开启短信验证后，注册页显示短信入口并能完成注册。

任何一项未通过，均不得将短信注册标记为正式可用。

## 9. 日常巡检

每周或每次变更后检查：

1. 阿里云账号是否欠费，短信服务是否可用。
2. 签名状态和移动、联通、电信的运营商报备结果是否异常或失效。
3. 模板是否仍为审核通过状态。
4. 最近发送量、成功率、失败码和异常增长。
5. Coolify 最近一次部署是否健康，`/api/status` 是否正常。
6. 邮箱注册兜底是否仍可用。
7. RAM 用户是否出现额外策略、异常 AccessKey 或异常调用。

建议监控项：短信发送失败率、每分钟请求数、供应商错误码分布、注册验证码完成率和短信费用突增。

## 10. 故障处理

先保存发生时间、脱敏手机号、运营商、应用日志中的阿里云 `code`、`message`、`request_id`，再按下表处理。不要记录验证码或 AccessKey。

| 现象或错误 | 判断 | 处理 |
| --- | --- | --- |
| `386`，提示签名实名制报备问题 | 对应运营商通道报备未完成，不等同于余额不足 | 检查签名关联资质和三网报备详情；按对应运营商进行少量真实测试并等待报备；期间关闭短信验证、保留邮箱验证 |
| `sms_verification_ready: false` | 应用未读到完整配置 | 检查五项环境变量并重新部署 |
| 权限不足、鉴权失败 | AccessKey、RAM 策略、密钥状态或归属不正确 | 核对 AK 所属 RAM 用户、`EbmSmsSendOnly` 绑定情况和是否存在优先级更高的 `Deny` |
| 签名或模板错误 | 环境变量与审核通过的名称/Code 不一致 | 逐字核对签名名称、模板 Code 和变量名 `code`，重新部署 |
| `429` 或“短信发送过于频繁” | 命中应用限流 | 根据 `Retry-After` 等待；不得通过换 IP 或批量号码绕过限制 |
| 页面显示发送失败，但阿里云有记录 | 阿里云拒绝请求或通道失败 | 以发送记录和应用日志中的准确错误码处理，不凭经验猜测 |
| 页面显示发送失败，阿里云无记录 | 请求可能未到阿里云 | 检查应用开关、参数、Turnstile、限流、网络和后端日志 |
| 怀疑余额不足 | 尚未形成结论 | 查看阿里云费用/余额和发送记录的准确失败原因；只有证据明确指向欠费时才充值 |
| 个别运营商收不到 | 该运营商报备或通道异常 | 按运营商拆分发送记录和测试，不以另一个运营商成功代替验收 |

### 10.1 紧急降级

当短信连续失败、成功率明显下降或供应商状态不明时：

1. 在“系统设置 → 身份验证 → 基本身份验证”关闭短信验证。
2. 确认邮箱验证保持开启，并实际测试一次邮箱注册。
3. 在阿里云发送记录和应用日志中定位准确错误码。
4. 修复后重新执行第 8 节验收，再开启短信验证。

关闭短信验证开关即可完成业务降级，不必为一般通道故障立即删除环境变量或 AccessKey。若怀疑密钥泄露，应立即禁用泄露的 AccessKey，并按第 11 节轮换。

## 11. AccessKey 轮换

建议至少每 90 天检查一次密钥使用情况，并在人员变更、疑似泄露或异常调用时立即轮换。

1. 确认 RAM 用户仍只有最小权限。
2. 创建第二组 AccessKey，存入密码管理工具。
3. 在 Coolify 中同时更新 `ALIBABA_CLOUD_ACCESS_KEY_ID` 和 `ALIBABA_CLOUD_ACCESS_KEY_SECRET`。
4. 重新部署并确认 `/api/status`、发送记录和一次真实收件测试均正常。
5. 禁用旧 AccessKey，观察无异常后删除旧 AccessKey。
6. 记录轮换日期、操作者、验证结果和旧密钥删除状态；记录中不得包含密钥值。

任何时候都不要先删除唯一可用的旧 AccessKey，再测试新 AccessKey。

## 12. 变更与留痕

以下变更必须重新部署并执行第 8 节验收：

- 更换短信签名、模板或关联企业资质。
- 修改 RAM 用户、权限策略或 AccessKey。
- 修改五项 Coolify 环境变量。
- 修改短信 SDK、发送参数、验证码逻辑、限流或注册流程。

每次变更至少记录：日期、原因、变更项、审批人、执行人、测试号码运营商、阿里云 Request ID、验收结果、回滚结果和应用版本。手机号必须脱敏，AccessKey 和验证码禁止入档。

## 13. 官方参考

- [签名实名制报备](https://help.aliyun.com/zh/sms/user-guide/real-name-reporting-of-sms-sign-name)
- [申请短信签名](https://help.aliyun.com/zh/sms/user-guide/create-signatures/)
- [申请短信模板](https://help.aliyun.com/zh/sms/user-guide/create-message-templates-1/)
- [验证码模板规范](https://help.aliyun.com/zh/sms/user-guide/verification-code-template-specifications/)
- [短信服务身份管理](https://help.aliyun.com/zh/sms/identity-management)
- [短信服务自定义权限策略参考](https://help.aliyun.com/zh/sms/custom-permission-policy-reference)
- [创建 RAM 用户](https://help.aliyun.com/zh/ram/user-guide/create-a-ram-user)
- [创建 AccessKey](https://help.aliyun.com/zh/ram/user-guide/create-an-accesskey-pair)
- [项目现有短信验证配置说明](./aliyun-sms-verification.md)
